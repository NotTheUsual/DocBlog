<section class="main-content">
  <table class="post-titles">
    <tr>
      <th>Post Title</th>
      <th>Date</th>
    </tr>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.date %></td>
      </tr>
    <% end %>
  </table>
</section>